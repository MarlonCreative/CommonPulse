"use client";
import { useEffect, type RefObject } from "react";
type Registry={registerTool:(tool:{name:string;description:string;inputSchema:object;annotations:{readOnlyHint:boolean;untrustedContentHint:boolean};execute:(input:unknown)=>unknown},options:{signal:AbortSignal})=>void|Promise<void>};
export function useNotebookTool(ref:RefObject<unknown>){useEffect(()=>{const registry=(document as Document&{modelContext?:Registry}).modelContext;if(!registry?.registerTool)return;const lifecycle=new AbortController();try{void Promise.resolve(registry.registerTool({name:"read_musical_notebook",description:"Read the current edited rhythm, field notes and measured sound events. Recording audio and API credentials are excluded. Reading does not save or change anything.",inputSchema:{type:"object",properties:{},additionalProperties:false},annotations:{readOnlyHint:true,untrustedContentHint:true},execute(input){if(!input||typeof input!=="object"||Array.isArray(input)||Object.keys(input).length)throw Error("Expected an empty object.");const notebook=ref.current as Record<string,unknown>|null;if(!notebook)return {ready:false};const {audio,...data}=notebook;return {ready:true,notebook:JSON.parse(JSON.stringify(data))};}},{signal:lifecycle.signal})).catch(()=>{});}catch{}return()=>lifecycle.abort();},[ref]);}
